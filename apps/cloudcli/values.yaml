image: kamatera/cloudcli-server:8d4ed418ca3c2a2b0de73a0818f020653a8e2bf3
minReplicas: 4
maxReplicas: 15
#cloudcliServerDomain: "cloudcli.cloudwm.com"
cloudcliServerDomain: "cloudcli.~iac:subdomain_prefix~.~iac:root_domain~"
nfs_private_ip: "~iac:nfs_private_ip~"

k8s:
  enabled: false
  # version is the git hash from Kamatera/cloudcli-server-kubernetes repo
  version: 983398bc412c84af04cbf9d430934b967d365b4f
  replicas: 3
  resources:
    requests:
      cpu: 200m
      memory: 256Mi
    limits:
      memory: 500Mi
  worker:
    replicas: 4
    terminationGracePeriodSeconds: 3600
    resources:
      requests:
        cpu: 200m
        memory: 256Mi
      limits:
        memory: 500Mi
  scheduler:
    resources:
      requests:
        cpu: 200m
        memory: 256Mi
      limits:
        memory: 500Mi
  flower:
    resources:
      requests:
        cpu: 200m
        memory: 256Mi
      limits:
        memory: 500Mi
  db:
    resources:
      requests:
        cpu: 200m
        memory: 256Mi
      limits:
        memory: 500Mi
  rabbitmq:
    resources:
      requests:
        cpu: 200m
        memory: 256Mi
      limits:
        memory: 500Mi

secrets:
- name: build-cloudcli-binaries
  data:
    AWS_ACCESS_KEY_ID: "~vault:cloudcli/build-env:AWS_ACCESS_KEY_ID~"
    AWS_SECRET_ACCESS_KEY: "~vault:cloudcli/build-env:AWS_SECRET_ACCESS_KEY~"
    AWS_REGION: "~vault:cloudcli/build-env:AWS_REGION~"
    AWS_MAC_INSTANCE_AVAILABILITY_ZONE: "~vault:cloudcli/build-env:AWS_MAC_INSTANCE_AVAILABILITY_ZONE~"
    AWS_MAC_INSTANCE_ID: "~vault:cloudcli/build-env:AWS_MAC_INSTANCE_ID~"
    AWS_MAC_PEM_KEY_BASE64: "~vault:cloudcli/build-env:AWS_MAC_PEM_KEY_BASE64~"
    SSH_IP: "~vault:cloudcli/build-env:SSH_IP~"
    SSH_PRIVATE_KEY_BASE64: "~vault:cloudcli/build-env:SSH_PRIVATE_KEY_BASE64~"
- name: cloudcli-k8s-celery
  data:
    POSTGRES_PASSWORD: "~vault:cloudcli/celery:POSTGRES_PASSWORD~"
    CELERY_RESULT_BACKEND: "~vault:cloudcli/celery:CELERY_RESULT_BACKEND~"
    CELERY_BROKER: "~vault:cloudcli/celery:CELERY_BROKER~"
- name: kamateratoolbox-pages
  data:
    GH_USER: "~vault:cloudcli/kamateratoolbox-pages:GH_USER~"
    GH_TOKEN: "~vault:cloudcli/kamateratoolbox-pages:GH_TOKEN~"
    GIT_USERNAME: "~vault:cloudcli/kamateratoolbox-pages:GIT_USERNAME~"
    GIT_EMAIL: "~vault:cloudcli/kamateratoolbox-pages:GIT_EMAIL~"
