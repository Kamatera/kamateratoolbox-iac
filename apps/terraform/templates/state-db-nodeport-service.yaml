apiVersion: v1
kind: Service
metadata:
  name: state-db-nodeport
spec:
  type: NodePort
  selector:
    app: state-db
  ports:
  - name: "31543"
    port: 31543
    targetPort: 5432
    nodePort: 31543
